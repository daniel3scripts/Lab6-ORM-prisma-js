/* function Daniel(x, y) {
    return x + y;
} */

export const sumar = (a, b) => Number(a) + Number(b);

export const restar = (a, b) => Number(a) - Number(b);

export const multi = (a, b) => Number(a) * Number(b);


export const divi = (a, b) => b === 0 ? 0 : Number(a) / Number(b);